# DirectoryReader abstraction layer

package(default_visibility = ["//visibility:public"])

# DirectoryReader library (skeleton only for now)
cc_library(
    name = "dir_reader",
    srcs = ["dir_reader.c"],
    hdrs = ["dir_reader.h"],
    # linkopts will include -luring when we add io_uring support
)

# Export headers
exports_files(["dir_reader.h"])

