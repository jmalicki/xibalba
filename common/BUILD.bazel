# DirectoryReader abstraction layer (minimal for tech de-risking)

package(default_visibility = ["//visibility:public"])

# Minimal DirectoryReader library (classic readdir only)
cc_library(
    name = "dir_reader",
    srcs = ["dir_reader.c"],
    hdrs = ["dir_reader.h"],
    # No liburing dependency for tech de-risking PoC
)

# Export headers
exports_files(["dir_reader.h"])

